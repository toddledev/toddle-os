on:
  push
  # inputs:
  #   package:
  #     description: 'Package to publish'
  #     required: true
  #     default: 'core'
  #     type: choice
  #     options:
  #       - core
  #       - lib
  #       - runtime
  #       - search
  #       - ssr

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout root
        uses: actions/checkout@v4
      - name: Install bun
        uses: oven-sh/setup-bun@v1
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Build
        run: bun run build
        # cd packages/${{ inputs.package }}
      - name: Pack
        run: |
          mkdir publish
          cd packages/ssr
          bun pm pack --destination ../../publish
          mv ../../publish/ssr-*.tgz ../../publish/ssr.tgz
      - uses: JS-DevTools/npm-publish@v3
        with:
          token: ${{ secrets.NPM_TOKEN }}
          package: publish/ssr.tgz
          access: public
      # - name: Publish
      #   run: |
      #     cd packages/ssr
      #     bun npm-publish
      #   env:
      #     NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
